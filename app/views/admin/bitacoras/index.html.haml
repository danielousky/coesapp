.border.mt-3.p-3
  %table.dataTable.table.table-striped.table-sm.table-hover.table-bordered
    %thead
      %tr
        %th Id
        %th Fecha
        %th Usuario
        %th Objeto
        %th Tipo Trans
        %th Descripción

    %tbody

      - @bitacoras.includes(:usuario).each do |bita| 
        %tr
          %td= bita.id
          %td
            = "#{(bita.created_at-4.hours).strftime('%d/%m/%Y %H:%M %p')}"
            =# "Hace #{distance_of_time_in_words(Time.zone.now.hour, bita.created_at.hour)} (#{(bita.created_at-4.hours).strftime('%d/%m/%Y %H:%M %p')})"
          %td= bita.usuario.descripcion if bita.usuario
          %td= bita.tipo_objeto
          %td= bita.tipo.capitalize if bita.tipo
          %td= bita.descripcion
