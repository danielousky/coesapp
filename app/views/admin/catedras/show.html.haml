.p-3
  - if @escuela
    %p
      %b Escuela:
      = @escuela.descripcion

  %p 
    %b Nombre:
    = @catedra.descripcion
  
  %p
    %b Departamentos:
    = @catedra.departamentos.map{|d| d.descripcion}.to_sentence

  %b Asignaturas
  - @asignaturas = @catedra.asignaturas
  - @escuelas = Escuela.where(id: @escuela.id)
  .border.p-3
    = render 'admin/asignaturas/index_colocar_tabla'

.form-group
  - if @escuela
    = link_to escuela_path(@escuela), class: 'btn btn-outline-secondary ml-2' do
      = glyph 'chevron-left'
      = t('.back', default: t("helpers.links.back"))
  
  - if current_admin.maestros?
    = link_to edit_catedra_path(@catedra), class: 'btn btn-outline-info ml-2' do
      = glyph 'edit'
      = t('.edit', default: t("helpers.links.edit"))

  - if current_admin.ninja? and !@catedra.asignaturas.any? and !@catedra.departamentos.any?
    = link_to catedra_path(@catedra), method: "delete", data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) }, class: 'btn btn-outline-danger ml-2' do
      = glyph 'trash'
      = t('.destroy', default: t("helpers.links.destroy"))
  - else
    .btn.btn-outline-secondary.disabled.ml-2.tooltip-btn{data: {togle: :tooltip}, title: 'Elimine las asignaturas y departamentos primero antes de eliminar la cátedra'}
      = glyph 'trash'