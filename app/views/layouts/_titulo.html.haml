

%h5.text-light.bg-dark.justify-content-center.align-middle.text-center.pt-5.border.border-info{style: "position: -webkit-sticky; position: sticky;top: 0;"}

	= link_to_if current_admin, "Periodo: #{current_periodo.id}", 'javascript:void(0)', {onclick: "return $('#cambiar_sesion_periodo').modal();", 'data_toggle': :modal, class: 'float-right text-info pt-md-2 mr-3'}

	= raw (@titulo ? @titulo : action_name.titleize)

	- if false #OJO: Se está retirando esta función ya que es indispinsble incluir la escuela a la cual se esá inscribiendo
		- if current_admin and current_admin.autorizado? 'inscripcionsecciones', 'inscribir' 
			= simple_toggle buscar_estudiante_inscripcionsecciones_path, '', 'Inscribir Estudiante', 'success float-left ml-md-1', 'education'
